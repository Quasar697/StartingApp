<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema per MainActivity -->
    <style name="Theme.StartingApp" parent="android:Theme.Material.Light.DarkActionBar">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <!-- Tema per ChatActivity -->
    <style name="Theme.StartingApp.NoActionBar" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

</resources>